# Dependency_Tracker
📦 Java Dependency Tracker A browser-native tool for visualizing Maven pom.xml and .jar relationships.  Deep Inspection: Uses JSZip to extract metadata directly from JARs.  Visual Graphs: Interactive SVG maps of parent-child dependencies.  Conflict Detection: Flags version shadowing &amp; mismatches.  Zero-Backend: 100% client-side React
